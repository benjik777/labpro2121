#include <stdio.h>

int main()
{
    int x = 7;
    int y = 3;
    printf ("%i + %i = %i\n", x, y, x+y);
    printf ("%i - %i = %i\n", x, y, x-y);
    printf ("%i / %i = %i\n", x, y, x/y);
    return 0;
}
